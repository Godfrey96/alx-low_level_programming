#include <stdio.h>
/**
 * main-Entry point
 * program to print base 10 numbers
 * Return: 0 if successful
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
